export enum ClaimRequestStatus {
    DRAFT = 'draft',
    PENDING = 'pending',
    APPROVED = 'approved',
    PAID = 'paid',
    REJECTED = 'rejected',
    CANCELLED = 'cancelled',
    RETURNED = 'returned'
}